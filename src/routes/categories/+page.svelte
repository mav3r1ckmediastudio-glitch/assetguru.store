<script lang="ts">import PageIntro from '$lib/components/PageIntro.svelte';import CategoryCard from '$lib/components/CategoryCard.svelte';import { categories } from '$lib/data/marketplace';</script>
<svelte:head><title>Categories — AssetGuru</title></svelte:head>
<PageIntro eyebrow="Explore the catalogue" title="Asset" highlight="categories" description="A GameGuru MAX-first taxonomy designed around real production workflows, not a generic ecommerce menu."/>
<section class="section"><div class="content-wrap"><div class="grid">{#each $categories as category}<CategoryCard {category}/>{/each}</div><div class="note glass"><h2>More categories are ready for the full build.</h2><p>Animations, UI & 2D, shaders and VFX, complete systems, developer tools, templates, bundles and free assets are already represented in the architecture plan.</p></div></div></section>
<style>.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:14px}.note{margin-top:28px;padding:28px;border-radius:15px}.note h2{margin-top:0}.note p{color:#aab5c8;line-height:1.65}@media(max-width:960px){.grid{grid-template-columns:repeat(2,1fr)}}@media(max-width:560px){.grid{grid-template-columns:1fr}}</style>
