<script lang="ts">import { page } from '$app/stores'; $: failed=$page.url.searchParams.get('state')==='failed'; $: expired=$page.url.searchParams.get('state')==='expired';</script>
<svelte:head><title>Verify your account — AssetGuru</title></svelte:head>
<section class="verify section"><div class="verify-card glass"><span class="eyebrow">Email verification</span><h1>{failed||expired?'That link is no longer valid.':'Check your inbox.'}</h1><p>{failed||expired?'Request a fresh password reset or sign in to resend account verification.':'Open the secure link sent by AssetGuru to finish activating your account.'}</p><div class="actions">{#if expired}<a class="button button-primary" href="/auth/forgot-password">Request a new link</a>{:else}<a class="button button-primary" href="/auth/login">Continue to sign in</a>{/if}<a class="button button-secondary" href="/">Return home</a></div></div></section>
<style>.verify{min-height:calc(100dvh - 160px);display:grid;place-items:center}.verify-card{width:min(620px,calc(100% - 28px));padding:40px;border-radius:18px;text-align:center}.verify-card h1{margin:15px 0;font-size:clamp(2.5rem,5vw,4.5rem);letter-spacing:-.055em}.verify-card p{margin:0 auto;max-width:500px;color:#aab5c8;line-height:1.7}.actions{margin-top:25px;display:flex;justify-content:center;gap:10px;flex-wrap:wrap}</style>
