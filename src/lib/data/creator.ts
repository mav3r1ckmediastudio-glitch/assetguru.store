export type ProductStatus = 'Published' | 'Draft' | 'In review' | 'Changes required' | 'Retired';
export type OrderStatus = 'Paid' | 'Pending' | 'Refund requested' | 'Refunded';
export type CreatorProduct = {
  id?: string;
  currentVersionId?: string;
  slug: string;
  title: string;
  image: string;
  category: string;
  subcategory: string;
  status: ProductStatus;
  price: number;
  sales: number;
  revenue: number;
  views: number;
  conversion: number;
  rating: number;
  reviews: number;
  version: string;
  updated: string;
  moderationNote?: string;
  summary: string;
  description: string;
  extendedPrice?: number;
  compatibility: string;
  maxVersion: string;
  sourceFiles: boolean;
  dependencies: string;
  performance: 'Lightweight' | 'Mid-range' | 'High detail';
  features: string[];
  contents: string[];
  tags: string[];
  formats: string[];
  licence: string;
  images: string[];
  showcaseVideoUrl?: string;
  versions: { id:string; version:string; status:string; isCurrent:boolean; size:string; created:string; releaseNotes:string }[];
};
export type CreatorOrder = {
  id: string;
  buyer: string;
  initials: string;
  product: string;
  licence: 'Standard' | 'Extended';
  date: string;
  createdAt: string;
  total: number;
  fee: number;
  net: number;
  status: OrderStatus;
};
